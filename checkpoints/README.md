# Checkpoints directory — model weights are stored here during training.
# This folder is excluded from git (see .gitignore).
# Files saved here:
#   best_rrdbnet.pth  ← best validation loss checkpoint
#   last_rrdbnet.pth  ← most recent epoch checkpoint
